[project]
name = "hebrew-latin-token-classifier"
version = "0.1.0"
description = "Corpus generation, annotation and review for a Hebrew-in-Latin-script token classifier"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "huggingface-hub>=0.24",
]
